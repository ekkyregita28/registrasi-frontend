'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = getInitStatus;

var _constants = require('./constants');

function getInitStatus(component, willEnterhooks) {
  if (!component || !component[_constants.routerHookPropName]) {
    return 'done';
  }
  var hooks = component[_constants.routerHookPropName];
  for (var i = 0; i < willEnterhooks.length; i += 1) {
    if (hooks[willEnterhooks[i]]) {
      return 'init';
    }
  }
  return 'defer';
}